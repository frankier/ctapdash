<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <runtime>
    <!-- Explorer can propagate a downloaded ZIP's Mark of the Web to the
         bundled .NET assemblies. Trust those assemblies in this application,
         without removing their zone markers or changing machine policy. -->
    <loadFromRemoteSources enabled="true" />
  </runtime>
</configuration>
